<div class="mt-10 p-4">
    <p> <bold> Lembrete: K-Fold precisam possuir K&quot;s maiores do que 1, K-Fold = 0 ou 1 desabilitam do K-fold </bold></p>
    <div>
        <%= form_with url: observation_put_kfold_path(@observation), method: :put do |form| %>
            <div class="inline-block">
                <%= form.label :kfold, "Quantidade de Folds", class:"inline-block text-gray-700 text-xl font-bold mb-2" %>
                <%= form.number_field :kfold, value: @observation.k_fold ? @observation.k_fold : 0, class:"inline-block text-base shadow appearance-none border rounded py-2 px-3 text-gray-700 mb-3 leading-tight focus:outline-none focus:shadow-outline" %>
            </div>

            <div class="actions m-4 ml-24 mt-10">
                <%= form.submit "Enviar", class:"inline-block bg-orange-500 hover:bg-orange-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline" %>
            </div>
          <% end %>
          <div class="inline-block bg-orange-500 hover:bg-orange-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline"><%= link_to 'Desabilitar', observation_delete_kfold_path(@observation), method: :delete %></div>
    </div>
</div>
