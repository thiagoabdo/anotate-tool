<%= form_with(model: ml_order, local: true) do |form| %>
  <% if ml_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ml_order.errors.count, "error") %> prohibited this ml_order from being saved:</h2>

      <ul>
        <% ml_order.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :entry_id %>
    <%= form.text_field :entry_id %>
  </div>

  <div class="field">
    <%= form.label :observation_id %>
    <%= form.text_field :observation_id %>
  </div>

  <div class="field">
    <%= form.label :order %>
    <%= form.number_field :order %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
