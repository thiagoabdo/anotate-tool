<%= form_with(model: ml_feature, local: true) do |form| %>
  <% if ml_feature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ml_feature.errors.count, "error") %> prohibited this ml_feature from being saved:</h2>

      <ul>
        <% ml_feature.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :belongs_to %>
    <%= form.text_field :belongs_to %>
  </div>

  <div class="field">
    <%= form.label :string %>
    <%= form.text_field :string %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
